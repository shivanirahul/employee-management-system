import EmployeeForm from '../../components/EmployeeForm/EmployeeForm';

// This is the component your route should render for the "Add Employee" page.
// The actual form (steps, validation, submit) lives in components/EmployeeForm.
export default function EmployeeAdd() {
  return <EmployeeForm />;
}